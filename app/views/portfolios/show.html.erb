<div class="card mb-4 shadow">
  <div class="card-body">
    <h1 class="card-title"><%= @portfolio.title %></h1>
    <p class="card-text"><%= @portfolio.description %></p>
    <a href="<%= @portfolio.url %>" class="btn btn-primary">Visit Portfolio</a>
  </div>
</div>
